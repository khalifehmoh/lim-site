import React, { useEffect } from "react"
import { connect } from "frontity"

const Assesment = ({ state }) => {
    // const data = state.source.get(state.router.link)

    return (
        <div>
            Assesment is working
        </div>
    )
}

export default connect(Assesment)